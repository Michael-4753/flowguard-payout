// Milestone workbench removed: milestone / outsourcing tracking is a
// business-team concern, not part of the cashier's payout control console, and
// it was disconnected from the actual payout flow (instructions are created in
// /pay). Kept as an empty module to avoid dangling imports; /milestones now
// redirects home.
export {};
