// Stablecoin wallet-backfill modal removed for compliance.
//
// The compliant flow collects no wallet addresses anywhere in the UI and the
// platform performs no crypto/stablecoin transfer, custody or exchange. Left as
// an empty module to avoid a dangling import.
export {};
