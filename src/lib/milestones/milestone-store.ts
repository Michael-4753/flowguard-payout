// Milestone workbench store removed: milestone / outsourcing tracking is a
// business-team concern and was disconnected from the actual payout flow
// (instructions are created in /pay). Kept as an empty module to avoid dangling
// imports; the /milestones route now redirects home.
export {};
