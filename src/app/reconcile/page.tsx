import { AppShell } from "@/components/shell/app-shell";
import { ReconcileScreen } from "@/components/screens/reconcile-screen";

export default function ReconcilePage() {
  return (
    <AppShell>
      <ReconcileScreen />
    </AppShell>
  );
}
