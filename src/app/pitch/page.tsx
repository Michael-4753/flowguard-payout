import { PitchDeck } from "@/components/pitch/pitch-deck";

export const metadata = {
  title: "FlowGuard — Investor Pitch",
  description: "Dual-route, risk-first cross-border payments. Check before you send.",
};

export default function PitchPage() {
  return <PitchDeck />;
}
